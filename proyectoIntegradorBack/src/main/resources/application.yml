spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/co2x
    username: postgres
    password: postgre

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  server:
    port: 5432